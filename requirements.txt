# URLLM 跨域序列推荐项目 - Python 依赖
# 安装: pip install -r requirements.txt
# Python 版本: 3.10
#
# 说明: torch/torchvision 需按你的 CUDA 版本调整,下面是 CUDA 12.1 版本
# 其他 CUDA 版本见 https://pytorch.org/get-started/previous-versions/

# === PyTorch(按 CUDA 版本选择) ===
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# === 大模型训练 / 推理 ===
transformers==5.14.1
peft==0.20.0
accelerate==1.14.0
datasets==5.0.1
# bitsandbytes: GM 数据集训练需要(8bit QLoRA); AO 数据集训练使用 FP16,不需要
bitsandbytes==0.50.0
safetensors==0.8.0
protobuf==7.35.1

# === 数据处理与评估 ===
numpy==2.2.6
pandas==2.3.3
scikit-learn==1.7.2
scipy==1.15.3
tqdm==4.70.0

# === 模型下载(国内环境) ===
modelscope==1.39.0
