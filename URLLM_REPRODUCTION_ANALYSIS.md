# URLLM 论文复现分析与改进方向

> 生成时间：2026-08-05
> 论文：*Exploring User Retrieval Integration towards Large Language Models for Cross-Domain Sequential Recommendation* (arXiv:2406.03085)
> 项目路径：`/root/autodl-tmp/URLLM-project/`

---

## 一、当前复现水平

### 1.1 已完成的工作

| 模块 | 状态 | 说明 |
|------|------|------|
| 数据预处理 | ✅ 完成 | GM + AO 双数据集，含交互序列、物品元数据、ID 映射 |
| 物品属性提取 | ✅ 完成 | DeepSeek 提取 170,239 物品属性（GM），用于 Jaccard 相似度扩展 |
| 用户画像构建 | ✅ 完成 | 行为特征 + 语义偏好，GM 40,479 用户 / AO 9,577 用户 |
| 指令数据构建 | ✅ 完成 | Alpaca 格式，GM 31,570 条 / AO 8,000 条 |
| LoRA 微调 | ✅ 完成 | GM 7,894 步 / AO 2,000 步，Llama2-7B |
| 推理 | ✅ 完成 | GM 3,601 条 / AO 1,000 条预测结果 |
| 评估 | ✅ 完成 | HR@K、NDCG@K、MRR、模糊匹配、OOD、DG 基线对比 |
| 前端展示 | ✅ 完成 | 4 页面，GM/AO 双数据集切换，全部真实数据 |
| 相似用户计算 | ✅ 完成 | 基于画像属性 Jaccard 相似度（仅展示用途） |

### 1.2 核心评估指标

#### GM 数据集（Movie → Game）

| 指标 | 本项目 (LLM+画像) | DG 基线 | 提升 |
|------|-------------------|---------|------|
| HR@1 | 0.0147 | 0.0000 | — |
| HR@5 | 0.0169 | 0.0000 | — |
| HR@10 | 0.0181 | 0.0000 | — |
| HR@20 | 0.0183 | 0.0003 | ~61 倍 |
| MRR | 0.0157 | 0.0001 | ~157 倍 |
| eval_loss | 0.4347 | — | — |
| eval_accuracy | 0.8401 | — | — |

#### AO 数据集（Office → Art）

| 指标 | 本项目 (LLM+画像) | DG 基线 | 提升 |
|------|-------------------|---------|------|
| HR@1 | 0.0020 | 0.0000 | — |
| HR@5 | 0.0030 | 0.0000 | — |
| HR@10 | 0.0030 | 0.0000 | — |
| HR@20 | 0.0040 | 0.0010 | 4 倍 |
| MRR | 0.0026 | 0.0003 | ~9 倍 |
| eval_loss | 0.4266 | — | — |
| OOD | 92.40% | — | — |

### 1.3 技术栈

- 基座模型：Llama-2-7b
- 微调方式：8bit QLoRA（GM）/ FP16（AO）
- LoRA 配置：r=16, alpha=32, 7 个注意力模块
- 前端：Vite + React + TypeScript + TailwindCSS + Recharts

---

## 二、与论文的差距

### 2.1 核心方法差距

论文的核心创新是 **User Retrieval Integration (URI)**——检索相似用户并将他们的目标域偏好融入 LLM 指令。本项目仅实现了用户画像增强，**未实现检索增强**，这是效果差距的根本原因。

| 环节 | 论文方法 | 本项目实现 | 差距 |
|------|---------|-----------|------|
| **指令构建** | 当前用户画像 + **Top-K 相似用户的目标域历史** | 仅当前用户画像 | ❌ 缺少相似用户信息 |
| **推理输入** | 当前用户画像 + **相似用户参考** | 仅当前用户画像 | ❌ 缺少检索增强 |
| **评估方式** | LLM 生成候选 → **检索匹配** | LLM 单条生成 → Jaccard 扩展 | ❌ 非论文原始评估 |
| **候选数量** | beam search 取 **多个候选** | beam search 仅取 top-1 | ❌ 候选不足 |
| **相似用户检索** | 基于 embedding 向量检索 | 基于画像属性 Jaccard | ⚠️ 检索方式不同 |
| **embedding 画像** | 完整的 DG 特征向量 | 计算低效，未补全 | ⚠️ 未完成 |

### 2.2 训练差距

| 维度 | 论文 | 本项目 (GM) | 本项目 (AO) |
|------|------|------------|------------|
| 训练步数 | 充分训练 | 7,894 步 | 2,000 步（不足） |
| 训练指令 | — | 31,570 条 | 8,000 条（不足） |
| 训练轮数 | — | 2 epoch | 2 epoch |
| 相似用户增强 | ✅ 有 | ❌ 无 | ❌ 无 |

### 2.3 效果差距分析

**AO 数据集效果较差的原因：**

1. **训练严重不足**：AO 仅 2,000 步 / 8,000 条指令（GM 的 1/4），模型未充分学习目标域物品
2. **物品标题复杂**：AO 物品标题含品牌名、型号、尺寸（如 "Sharpie permanent markers fine point purple"），LLM 难以精确生成
3. **域外率高**：OOD 92.4%，模型倾向推荐源域物品而非目标域
4. **缺少检索增强**：没有相似用户的目标域历史作为参考，模型无法借鉴相似用户的跨域偏好
5. **跨域关联弱**：Office→Art 的兴趣迁移路径比 Movie→Game 更迂回

---

## 三、改进方法

### 3.1 实现用户检索增强（最关键）

**这是论文的核心创新，不实现此功能无法达到论文水平。**

**实现步骤：**

1. **相似用户检索**：对每个用户，基于 embedding 向量检索 Top-K 相似用户
2. **指令模板修改**：在 [build_instruction_data.py](file:///root/autodl-tmp/URLLM-project/enhancement/src/build_instruction_data.py) 中，将相似用户的目标域交互物品标题拼接到指令 input
3. **训练指令格式**：
   ```
   ### Instruction:
   你是一个跨域推荐系统。根据以下用户信息和相似用户的偏好，推荐一个目标域物品。
   
   ### Input:
   用户画像：{当前用户画像}
   相似用户参考：
   - 相似用户1（相似度0.43）：在目标域交互过 {物品A}, {物品B}, {物品C}
   - 相似用户2（相似度0.33）：在目标域交互过 {物品D}, {物品E}
   
   ### Output:
   {目标域物品标题}
   ```
4. **推理同步修改**：推理时也需检索相似用户并拼接到输入

**预期效果**：相似用户的目标域历史为 LLM 提供了直接的参考候选，大幅降低域外率，提升 HR@K。

### 3.2 改用检索式评估

**当前问题**：LLM 只生成 1 条预测，HR@1=HR@5=HR@10=HR@20，靠 Jaccard 扩展才有区分度。

**改进方法**：
1. 推理时 beam search 取 top-K 个候选（如 num_beams=20，返回所有 beam 结果）
2. 用物品属性 Jaccard 相似度对 K 个候选拍序
3. 用排好序的 K 个候选计算 HR@K / NDCG@K / MRR

**预期效果**：HR@K 自然递增，更贴近论文评估方式，指标更有说服力。

### 3.3 补全 embedding 画像

**当前问题**：[user_profile_builder.py](file:///root/autodl-tmp/URLLM-project/enhancement/src/user_profile_builder.py) 的 embedding 画像计算低效，未真正用于相似用户检索。

**改进方法**：
1. 用 DG 基线模型的编码器对用户交互序列编码，生成用户 embedding 向量
2. 构建 embedding 画像文件（如 `embedding_profiles.json`）
3. 基于向量余弦相似度检索 Top-K 相似用户（替代当前的属性 Jaccard）

**预期效果**：相似用户检索更精准，为检索增强提供更好的输入。

### 3.4 增加训练量

| 数据集 | 当前 | 目标 |
|--------|------|------|
| AO 训练步数 | 2,000 | 8,000+ |
| AO 训练指令 | 8,000 | 30,000+ |
| AO epochs | 2 | 3-5 |

**预期效果**：模型充分学习目标域物品标题，提升精确匹配率。

### 3.5 降低域外率

**当前问题**：AO OOD 92.4%，模型大量推荐源域物品。

**改进方法**：
1. 在指令模板中强化目标域上下文（如明确提示"请推荐 Art 域的物品"）
2. 增加目标域物品在训练指令中的曝光比例
3. 在推理时对生成结果做域过滤（后处理）

### 3.6 改进优先级

| 优先级 | 改进项 | 预期提升 | 难度 |
|--------|--------|---------|------|
| P0 | 实现用户检索增强 | 大幅提升 HR@K | 中 |
| P1 | 改用检索式评估 | 指标更合理 | 低 |
| P1 | 增加训练量 | 中等提升 | 低 |
| P2 | 补全 embedding 画像 | 提升检索精度 | 中 |
| P2 | 降低域外率 | 中等提升 | 低 |
| P3 | 扩大候选生成 | 自然递增 HR@K | 低 |

---

## 四、总结

本项目完成了论文的基础复现（用户画像增强 + LoRA 微调 + 评估 + 前端展示），但**未实现论文的核心创新——用户检索增强（URI）**。这是当前效果与论文差距的根本原因。

**最关键的改进**：在指令构建和推理阶段融入相似用户的目标域历史，让 LLM 不仅依赖当前用户画像，还能参考相似用户的跨域偏好。这一改进预期将大幅提升 HR@K，尤其是 AO 数据集的效果。

---

*文档结束*
